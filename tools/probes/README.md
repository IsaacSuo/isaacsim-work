# Isaac Sim / PhysX API 探针

这里保存一次性能力探索脚本，不属于生产运行入口。

- `cloth/`：Isaac Sim 6.0 表面/体积可变形 API 的连续探索记录。
- `physx/`：粒子发射、粒子回收和底层接口能力验证。

这些脚本可能直接在模块级启动 `SimulationApp`，应使用 `Y:\isaacsim\python.bat` 单独运行。不要从生产脚本中导入它们。

